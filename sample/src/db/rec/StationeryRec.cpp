// DO NOT EDIT: generated by whoa-autocode
#include "db/rec/StationeryRec.hpp"
#include "util/SFile.hpp"

const char* StationeryRec::GetFilename() {
    return "DBFilesClient\\Stationery.dbc";
}

uint32_t StationeryRec::GetNumColumns() {
    return 4;
}

uint32_t StationeryRec::GetRowSize() {
    return 16;
}

bool StationeryRec::NeedIDAssigned() {
    return false;
}

int32_t StationeryRec::GetID() {
    return this->m_ID;
}

void StationeryRec::SetID(int32_t id) {
    this->m_ID = id;
}

bool StationeryRec::Read(SFile* f, const char* stringBuffer) {
    uint32_t textureOfs;

    if (
        !SFile::Read(f, &this->m_ID, sizeof(this->m_ID), nullptr, nullptr, nullptr)
        || !SFile::Read(f, &this->m_itemID, sizeof(this->m_itemID), nullptr, nullptr, nullptr)
        || !SFile::Read(f, &textureOfs, sizeof(uint32_t), nullptr, nullptr, nullptr)
        || !SFile::Read(f, &this->m_flags, sizeof(this->m_flags), nullptr, nullptr, nullptr)
    ) {
        return false;
    }

    if (stringBuffer) {
        this->m_texture = &stringBuffer[textureOfs];
    } else {
        this->m_texture = "";
    }

    return true;
}
