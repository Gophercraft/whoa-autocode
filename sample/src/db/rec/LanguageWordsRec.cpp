// DO NOT EDIT: generated by whoa-autocode
#include "db/rec/LanguageWordsRec.hpp"
#include "util/SFile.hpp"

const char* LanguageWordsRec::GetFilename() {
    return "DBFilesClient\\LanguageWords.dbc";
}

uint32_t LanguageWordsRec::GetNumColumns() {
    return 3;
}

uint32_t LanguageWordsRec::GetRowSize() {
    return 12;
}

bool LanguageWordsRec::NeedIDAssigned() {
    return false;
}

int32_t LanguageWordsRec::GetID() {
    return this->m_ID;
}

void LanguageWordsRec::SetID(int32_t id) {
    this->m_ID = id;
}

bool LanguageWordsRec::Read(SFile* f, const char* stringBuffer) {
    uint32_t wordOfs;

    if (
        !SFile::Read(f, &this->m_ID, sizeof(this->m_ID), nullptr, nullptr, nullptr)
        || !SFile::Read(f, &this->m_languageID, sizeof(this->m_languageID), nullptr, nullptr, nullptr)
        || !SFile::Read(f, &wordOfs, sizeof(uint32_t), nullptr, nullptr, nullptr)
    ) {
        return false;
    }

    if (stringBuffer) {
        this->m_word = &stringBuffer[wordOfs];
    } else {
        this->m_word = "";
    }

    return true;
}
