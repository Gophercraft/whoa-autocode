// DO NOT EDIT: generated by whoa-autocode
#ifndef DB_SPAMMESSAGES_REC_HPP
#define DB_SPAMMESSAGES_REC_HPP

#include "util/SFile.hpp"
#include <cstdint>

struct SpamMessagesRec {
	static constexpr uint32_t columnCount = 2;
	static constexpr uint32_t rowSize = 8;
	static constexpr bool indexIsID = false;

	int32_t m_ID;
	const char* m_Text;

	static const char* GetFilename() {
		return "DBFilesClient\\SpamMessages.dbc";
	}

	bool Read(SFile* f, const char* stringBuffer) {
		uint32_t TextOfs;

		if (
			!SFile::Read(f, &this->m_ID, sizeof(this->m_ID), nullptr, nullptr, nullptr)
			|| !SFile::Read(f, &TextOfs, sizeof(uint32_t), nullptr, nullptr, nullptr)
		) {
			return false;
		}

		if (stringBuffer) {
			this->m_Text = &stringBuffer[TextOfs];
		} else {
			this->m_Text = "";
		}

		return true;
	}
};

#endif