// DO NOT EDIT: generated by whoa-autocode
#include "db/rec/SoundFilterRec.hpp"
#include "util/SFile.hpp"

const char* SoundFilterRec::GetFilename() {
    return "DBFilesClient\\SoundFilter.dbc";
}

uint32_t SoundFilterRec::GetNumColumns() {
    return 2;
}

uint32_t SoundFilterRec::GetRowSize() {
    return 8;
}

bool SoundFilterRec::NeedIDAssigned() {
    return false;
}

int32_t SoundFilterRec::GetID() {
    return this->m_ID;
}

void SoundFilterRec::SetID(int32_t id) {
    this->m_ID = id;
}

bool SoundFilterRec::Read(SFile* f, const char* stringBuffer) {
    uint32_t nameOfs;

    if (
        !SFile::Read(f, &this->m_ID, sizeof(this->m_ID), nullptr, nullptr, nullptr)
        || !SFile::Read(f, &nameOfs, sizeof(uint32_t), nullptr, nullptr, nullptr)
    ) {
        return false;
    }

    if (stringBuffer) {
        this->m_name = &stringBuffer[nameOfs];
    } else {
        this->m_name = "";
    }

    return true;
}
